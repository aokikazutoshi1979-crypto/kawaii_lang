// 画面ごとの「モード切替トグル」を表示するかどうか
class QuizModeToggleConfig {
  static const showInCategorySelection = true;   // ← まずはここだけ true
  static const showInQuestionList      = false;  // 後で切替したくなったら true に
  static const showInChat              = false;  // 同上
}
